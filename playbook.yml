- name: Dotfiles
  hosts: localhost
  connection: local

  tasks:
    - name: hello world
      debug:
        msg: "Im ready and ..."
    - name: All not requements apps delete
      include_tasks: roles/debloat.yml
    - name: Requements repo enabled
      include_tasks: roles/repo.yml
    - name: system for requements app install
      include_tasks: roles/install.yml
